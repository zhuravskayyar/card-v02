# 🎮 Система Прокачки Карт - Швидкий Старт

## Що реалізовано?

Повна система прокачки карт, де лишні дублікати використовуються як матеріал для апгрейду основної карти в деці.

### ✨ Основні фіч:

1. **Інвентар** - рахунок всіх копій кожної карти (дека + колекція)
2. **Лишні копії** - автоматичний розрахунок дублікатів
3. **Вартість апгрейду** - лінійно зростає (1→1, 2→2, 3→3...)
4. **Перевірка можливості** - функція визначає чи можна апгрейдити
5. **Виконання апгрейду** - списує дублікати, підвищує рівень
6. **Зелена стрілка ↑** - показується на апгрейджвальних картах
7. **Гаряча підказка** - "Поліпшуйте карти!" світиться коли є апгрейди
8. **Клік-апгрейд** - клік на карту = миттєвий апгрейд

---

## 🚀 Як це використовувати?

### Для Гравців:

1. Реєструєтеся / логіньтеся
2. Переходите на сторінку колоди
3. На картах з дублікатами бачите зелену стрілку ↑
4. Клікаєте на карту → вона апгрейджується
5. Дублікат автоматично видаляється з колекції
6. Рівень карти підвищується

### Для Розробників:

```javascript
// Отримати інвентар
const inventory = navigation.getInventory(profile);

// Перевірити чи можна апгрейдити карту
if (navigation.canUpgradeCard(deckItem, inventory)) {
  // Виконати апгрейд
  navigation.performUpgrade(deckItem, inventory, profile);
}

// Дізнатись вартість апгрейду
const cost = navigation.getUpgradeCost(deckItem.level);

// Дізнатись кількість лишніх копій
const extra = navigation.getExtraCopies(inventory, cardId);
```

---

## 📦 Структура Даних

### Інвентар (підраховується автоматично)
```javascript
{
  "C-F-001": 3,    // вогняна карта #1
  "C-W-002": 1,    // водяна карта #2
  "C-A-001": 2,    // повітряна карта #1
}
```

### Дека (в профілі)
```javascript
deckCards = [
  { id: "C-F-001", level: 2 },
  { id: "C-W-002", level: 1 },
  // ... 7 слотів
]
```

### Колекція (в профілі)
```javascript
collectionCards = [
  { id: "C-F-001", level: 1 },  // дублікат
  { id: "C-F-001", level: 1 },  // ще дублікат
  { id: "C-W-003", level: 1 },  // інша карта
  // ... 7 слотів
]
```

---

## 🎯 Механіка Прокачки

| Рівень | Апгрейд до | Потребує дублікатів |
|--------|-----------|-------------------|
| 1      | 2         | 1                 |
| 2      | 3         | 2                 |
| 3      | 4         | 3                 |
| 4      | 5         | 4                 |
| ...    | ...       | ...               |

**Приклад:**
- Карта в деці: `C-F-001` (lvl 1)
- Всього копій: 3 (1 в деці + 2 в колекції)
- Лишні копії: 2
- Вартість апгрейду: 1
- Можна апгрейдити? **ДА** (є 2, потребує 1)

↓ После апгрейду ↓

- Карта в деці: `C-F-001` (lvl **2**)
- Всього копій: 2 (1 в деці + 1 в колекції)
- Лишні копії: 1
- Вартість апгрейду: 2
- Можна апгрейдити? **НІ** (є 1, потребує 2)

---

## 🎨 CSS Стилі

### Класс `.upgradable`
Додається до карти коли вона может бути апгрейджена.

```css
.sp-card.upgradable::after {
  content: "↑";
  position: absolute;
  top: -12px;
  right: 8px;
  width: 26px;
  height: 26px;
  background: linear-gradient(180deg, #a6ff7a, #2ea13a);
  border-radius: 999px;
  border: 1px solid rgba(0,0,0,.45);
  box-shadow: 0 6px 14px rgba(0,0,0,.6), 0 0 16px rgba(90,255,120,.25);
  animation: pulse-upgrade 1.5s ease-in-out infinite;
}
```

### Класс `.hint.hot`
Додається до підказки коли є апгрейджвальні карти.

```css
.hint.hot {
  border-color: rgba(120,255,160,.35);
  box-shadow: inset 0 0 14px rgba(0,0,0,.65), 0 0 18px rgba(90,255,120,.18);
  color: rgba(220,255,220,.95);
}
```

---

## 📝 Функції в `navigation`

### `getInventory(profile)` 📦
Будує карту всіх копій карт з профілю.

**Параметри:**
- `profile` - профіль користувача

**Повертає:**
- Об'єкт `{ cardId: количество, ... }`

---

### `getExtraCopies(inventory, cardId)` ➕
Рахує лишні копії (не в деці).

**Параметри:**
- `inventory` - результат `getInventory()`
- `cardId` - ID карти

**Повертає:**
- Число лишніх копій

---

### `getUpgradeCost(level)` 💎
Визначає вартість апгрейду з поточного рівня.

**Параметри:**
- `level` - поточний рівень карти

**Повертає:**
- Число дублікатів які потребує

---

### `canUpgradeCard(deckItem, inventory)` ✅
Перевіряє чи можна апгрейдити карту.

**Параметри:**
- `deckItem` - карта з деки `{ id, level }`
- `inventory` - результат `getInventory()`

**Повертає:**
- `true` якщо можна, `false` якщо ні

---

### `hasAnyUpgradable(deck, inventory)` 🔍
Перевіряє чи в деці хоча б одна апгрейджвальна карта.

**Параметри:**
- `deck` - масив карт з деки
- `inventory` - результат `getInventory()`

**Повертає:**
- `true` якщо є, `false` якщо ні

---

### `performUpgrade(deckItem, inventory, profile)` ⚡
Виконує апгрейд карти.

**Параметри:**
- `deckItem` - карта з деки
- `inventory` - результат `getInventory()`
- `profile` - профіль користувача

**Дія:**
1. Списує дублікати з колекції
2. Підвищує рівень карти
3. Зберігає профіль

**Повертає:**
- `true` якщо успішно, `false` якщо не вдалося

---

## 🔗 Файли Документації

- **UPGRADE_SYSTEM.md** - детальна документація (ви читаєте)
- **UPGRADE_EXAMPLES.js** - приклади кода для розробників
- **test-upgrade.html** - інтерактивний тест механіки

---

## 🧪 Тестування

### Швидка Перевірка

Відкрити консоль (F12) і запустити:

```javascript
// 1. Перевірити інвентар
navigation.getInventory(userProfile.getProfile())

// 2. Перевірити першу карту
const profile = userProfile.getProfile();
const inv = navigation.getInventory(profile);
const first = profile.deckCards[0];
console.log({
  card: first,
  extra: navigation.getExtraCopies(inv, first.id),
  cost: navigation.getUpgradeCost(first.level),
  canUpgrade: navigation.canUpgradeCard(first, inv)
})

// 3. Апгрейдити першу карту
navigation.performUpgrade(first, inv, profile);
navigation.loadDeckCards(); // Перерендерити

// 4. Перевірити дані в localStorage
JSON.parse(localStorage.getItem('elem_users'))
```

---

## 💡 Поради

1. **Сила Колоди** - з'являється на головній сторінці, оновлюється при апгрейді
2. **Ефективність** - збирайте дублікати для апгрейду рідких карт
3. **Прогрес** - відстежуйте стан підказки - вона розповідає чи є чим займатись
4. **Збереження** - все автоматично зберігається у localStorage

---

## 🐛 Розв'язання Проблем

### Стрілки не з'являються
- Перезавантажте сторінку (Ctrl+Shift+R)
- Перевірте чи у вас є дублікати в колекції
- Відкрийте консоль (F12) і перевірте `navigation.getInventory()`

### Апгрейд не працює
- Перевірте вартість: `navigation.getUpgradeCost(level)`
- Перевірте лишні копії: `navigation.getExtraCopies(inv, cardId)`
- Прочитайте помилки в консолі (F12)

### Дані не зберігаються
- Перевірте чи не блокуються localStorage в браузері
- Спробуйте інший браузер
- Очистіть кеш браузера

---

**Остання оновлення:** 10 січня 2026 г.
