# 🎴 ПРИНЦИП РЕНДЕРУ КАРТ - СТИМПАНК (ОНОВЛЕНО)

## ✅ АКТУАЛЬНА ВЕРСІЯ 2.0

**Дата оновлення:** 10 січня 2026  
**Статус:** Активна система для 240 карт  
**Підтримка:** 4 стихії, 6 рівнів рідкості, 40 фракцій

---

## 📋 ЗМІНИ В НОВІЙ ВЕРСІЇ

### Що змінилося:
- ✅ **Стихії:** 6 → **4** (fire, water, air, earth)
- ✅ **Рідкості:** 4 → **6** (R1-R6: Звичайна → Міфічна)
- ✅ **Карти:** 16 → **240** унікальних карт
- ✅ **Фракції:** Додано систему 40 фракцій
- ✅ **Статистика:** Замість `power` → `attack` + `defense`
- ✅ **Компоненти:** Додано плашку фракції та оновлену статистику

---

## СТРУКТУРА КАРТИ

### Фізичні розміри
- **Desktop:** 160px × 240px
- **Tablet:** 140px × 210px  
- **Mobile:** 120px × 180px
- **Граничні радіуси:** 16px

---

## КОМПОНЕНТИ КАРТИ

### 1️⃣ **ФОН КАРТИ**
```css
Багатошаровий градієнт:
├─ Радіальне свічення (200px 180px at 40% 20%)
│  └─ rgba(255,220,140,.14) до прозорого
├─ Лінійний градієнт темноти (180deg)
│  └─ rgba(0,0,0,.18) → rgba(0,0,0,.72)
├─ Основна палітра (180deg)
│  └─ #1b120c → #080604
└─ Тінь з кольором стихії
   └─ color-mix(--elem, 22%)
```

**CSS змінні:**
- `--elem` → колір стихії (4 стихії)
- `--rar` → колір рідкості (6 рівнів)

---

### 2️⃣ **МЕТАЛЕВА РАМКА (::before pseudo-element)**
Створює 3D ефект борту карти:
```css
Положення: inset 8px (від краю)
Градієнт: 135deg
├─ Латунь з рідкістю (30% прозорості)
├─ Мідь з рідкістю (20% прозорості)  
└─ Темна основа з рідкістю (15% прозорості)
```

---

### 3️⃣ **ВНУТРІШНЯ ПЛАСТИНА (::after pseudo-element)**
Основний об'єм карти:
```css
Положення: inset 14px
Шари:
├─ Радіальне свічення (400px 320px at 50% 20%)
│  └─ rgba(255,210,120,.08)
├─ Лінійна темнота (180deg)
│  └─ rgba(255,210,120,.04) → rgba(0,0,0,.40)
└─ Основна залізниця (180deg)
   └─ #241a12 → #120c08
```

---

### 4️⃣ **ШЕСТЕРНЯ В ЛІВОМУ ВЕРХНЬОМУ КУТІ**
```css
Положення: top 14px, left 14px
Розмір: 44px × 44px (мобіль: 36px)

КОНСТРУКЦІЯ:
├─ Кругова основа (conic-gradient)
│  └─ Чергуюча латунь: 15deg/30deg сегменти
│
├─ Внутрішній центр (::before)
│  └─ Сфера з градієнтом
│
└─ SVG іконка стихії (20px × 20px)
   ├─ Колір: var(--elem)
   ├─ Фільтр: drop-shadow + glow
   └─ Анімація: rotate(180deg) при :hover
```

**Стихійні іконки (4 стихії):**
- 🔥 **Fire** - полум'я з язиками
- 💧 **Water** - крапля води
- 💨 **Air** - хвилі вітру
- 🌍 **Earth** - гора/кристал

---

### 5️⃣ **БЕЙДЖ РІДКОСТІ (НОВИЙ)**
```css
Положення: top 12px, right 12px
Розмір: автоматичний за контентом

СТИЛЬ:
├─ Фон: градієнт з кольором рідкості
├─ Текст: білий (bold, uppercase)
├─ Розмір шрифту: 9px
└─ Тінь: box-shadow + text-shadow
```

**6 рівнів рідкості:**
- **R1** - Звичайна (#b8a27b)
- **R2** - Незвичайна (#7aaa6f)
- **R3** - Рідкісна (#6fb2ff)
- **R4** - Епічна (#b07cff)
- **R5** - Легендарна (#ffcc66)
- **R6** - Міфічна (#ff6b9d)

---

### 6️⃣ **НАЗВА КАРТИ (ОНОВЛЕНО)**
```css
Положення: top 70px, left/right 14px
Розмір: автоматичний за шириною

СТИЛЬ:
├─ Фон: темний градієнт з прозорістю
├─ Бордер: колір стихії (40% прозорості)
├─ Текст: #f4e6c6 (12px, bold)
└─ Обмеження: ellipsis при переповненні
```

---

### 7️⃣ **ПЛАШКА ФРАКЦІЇ (НОВА)**
```css
Положення: top 115px, left/right 14px
Розмір: автоматичний за шириною

СТИЛЬ:
├─ Фон: темно-коричневий градієнт
├─ Текст: #d4c4a8 (9px, medium)
├─ Прозорість: 0.8
└─ Обмеження: ellipsis при переповненні
```

**Приклади фракцій:**
- F01: Орден Попелястого Сонця
- F11: Королівство Глибин
- F21: Небесні Кочівники
- F31: Кам'яні Домініони

---

### 8️⃣ **ПЛАШКА СТАТИСТИКИ (ОНОВЛЕНО)**
```css
Положення: bottom 14px, left/right 14px
Розмір: повна ширина × автоматична висота

СТРУКТУРА:
├─ Контейнер stats-plate
│  ├─ Фон: градієнт з дерева
│  ├─ Бордер: колір стихії (50% прозорості)
│  └─ Тінь + внутрішнє світло
│
└─ 2 статистики (flex, justify-space-around)
   ├─ Атака (⚔️ + значення)
   └─ Захист (🛡️ + значення)
```

**Відображення статистики:**
- Іконка: 16px (емодзі)
- Значення: 14px (bold, світиться)
- Колір тексту: #f4e6c6
- Ефект свічення: var(--elem)

---

## CSS КЛАСИ ТА СТРУКТУРА

### Основна HTML структура
```html
<div class="sp-card {element} {rarity}">
  <div class="rarity-badge">{rarityDisplay}</div>
  <div class="corner-gear">
    <svg>{elementIcon}</svg>
  </div>
  <div class="card-name-plate">{name}</div>
  <div class="faction-badge">{factionName}</div>
  <div class="stats-plate">
    <div class="stat-item">
      <span class="stat-icon">⚔️</span>
      <span class="stat-value">{attack}</span>
    </div>
    <div class="stat-item">
      <span class="stat-icon">🛡️</span>
      <span class="stat-value">{defense}</span>
    </div>
  </div>
</div>
```

### Модифікатори класів
```css
/* Стихії (4) */
.sp-card.fire   → --elem: #c45a2a
.sp-card.water  → --elem: #3b6c8e
.sp-card.air    → --elem: #9fb6c1
.sp-card.earth  → --elem: #7a6a3a

/* Рідкості (6) */
.sp-card.R1 → --rar: #b8a27b (Звичайна)
.sp-card.R2 → --rar: #7aaa6f (Незвичайна)
.sp-card.R3 → --rar: #6fb2ff (Рідкісна)
.sp-card.R4 → --rar: #b07cff (Епічна)
.sp-card.R5 → --rar: #ffcc66 (Легендарна)
.sp-card.R6 → --rar: #ff6b9d (Міфічна)

/* Стани */
.sp-card:hover → трансформація + свічення
.sp-card.selected → обводка кольором стихії
.sp-card.disabled → opacity + grayscale
```

---

## АНІМАЦІЇ ТА ЕФЕКТИ

### 1. Hover ефект
```css
transform: translateY(-8px) scale(1.02);
box-shadow: 0 12px 24px rgba(0,0,0,0.5),
            0 0 20px var(--elem);
```

### 2. Обертання шестерні
```css
.sp-card:hover .corner-gear {
  transform: rotate(180deg);
  transition: 0.6s cubic-bezier(0.4, 0, 0.2, 1);
}
```

### 3. Свічення рідкісних карт
```css
/* Для R5 та R6 */
@keyframes shimmer {
  0%   { background-position: -200% 0; }
  100% { background-position: 200% 0; }
}

.sp-card.R5, .sp-card.R6 {
  animation: shimmer 3s infinite linear;
}
```

### 4. Посилене свічення при hover (R5, R6)
```css
.sp-card.R5:hover, .sp-card.R6:hover {
  box-shadow: 
    0 12px 24px rgba(0,0,0,0.5),
    0 0 30px var(--rar),
    0 0 60px color-mix(in srgb, var(--rar) 40%, transparent);
}
```

---

## ІНТЕГРАЦІЯ З КОДОМ

### CardView.js - Рендеринг компонент
```javascript
import { ELEMENT_ICONS } from './constants.js';

export const createCardView = (card, options = {}) => {
  const cardEl = dom.create('div', {
    className: `sp-card ${card.element} ${card.rarity}`,
    'data-id': card.id
  });
  
  // Бейдж рідкості
  const rarityBadge = dom.create('div', {
    className: 'rarity-badge'
  }, [card.rarityDisplay]);
  
  // Шестерня
  const cornerGear = dom.create('div', { 
    className: 'corner-gear' 
  });
  cornerGear.innerHTML = `<svg>${ELEMENT_ICONS[card.element]}</svg>`;
  
  // Назва
  const cardName = dom.create('div', {
    className: 'card-name-plate'
  }, [card.name]);
  
  // Фракція
  const factionBadge = dom.create('div', {
    className: 'faction-badge'
  }, [card.factionName]);
  
  // Статистика
  const statsPlate = dom.create('div', { 
    className: 'stats-plate' 
  });
  // ... додавання attack/defense
  
  return cardEl;
};
```

### card-renderer.js - Рендеринг HTML
```javascript
class CardRenderer {
  render(cardData) {
    const { element, rarity, name, factionName, attack, defense } = cardData;
    const icon = this.elementIcons[element];
    
    return `
      <div class="sp-card ${element} ${rarity}">
        <div class="rarity-badge">${cardData.rarityDisplay}</div>
        <div class="corner-gear"><svg>${icon}</svg></div>
        <div class="card-name-plate">${name}</div>
        <div class="faction-badge">${factionName}</div>
        <div class="stats-plate">
          <div class="stat-item">
            <span class="stat-icon">⚔️</span>
            <span class="stat-value">${attack}</span>
          </div>
          <div class="stat-item">
            <span class="stat-icon">🛡️</span>
            <span class="stat-value">${defense}</span>
          </div>
        </div>
      </div>
    `;
  }
}
```

---

## ФАЙЛИ СИСТЕМИ

### CSS
- **steampunk-cards.css** - основні стилі карт
- Підключення: `<link rel="stylesheet" href="css/steampunk-cards.css">`

### JavaScript
- **card-renderer.js** - клас для рендерингу HTML
- **CardView.js** - DOM компонент для рендерингу
- **cards.js** - база 240 карт

### Тестові сторінки
- **test-steampunk-render.html** - демонстрація нового рендеру
- Запуск: `http://localhost:8000/test-steampunk-render.html`

---

## АДАПТИВНІСТЬ

### Брейкпоінти
```css
/* Desktop (за замовчуванням) */
.sp-card: 160px × 240px

/* Tablet (≤1024px) */
@media (max-width: 1024px) {
  .sp-card: 140px × 210px
  .corner-gear: 40px × 40px
}

/* Mobile (≤640px) */
@media (max-width: 640px) {
  .sp-card: 120px × 180px
  .corner-gear: 36px × 36px
}
```

---

## ТЕСТУВАННЯ

### Команди запуску
```bash
# Запуск сервера
run_game.bat

# Відкрити тестову сторінку
http://localhost:8000/test-steampunk-render.html
```

### Перевірка
- ✅ Всі 240 карт рендеряться коректно
- ✅ 4 стихії мають унікальні іконки
- ✅ 6 рівнів рідкості відрізняються кольорами
- ✅ Фракції відображаються
- ✅ Атака та захист коректно показані
- ✅ Hover ефекти працюють
- ✅ Обертання шестерні при hover
- ✅ Свічення для R5/R6

---

## ПІДСУМОК

✅ **Оновлено:** Система рендеру для 240 карт  
✅ **Стихії:** 4 (fire, water, air, earth)  
✅ **Рідкості:** 6 (R1-R6)  
✅ **Фракції:** 40 унікальних фракцій  
✅ **Статистика:** attack + defense  
✅ **Стиль:** Стимпанк з металевими рамками та шестернями  
✅ **Анімації:** Hover, обертання, свічення  

**Версія:** 2.0  
**Дата:** 10 січня 2026  
**Статус:** Активна ✅

**Число сили:**
- Шрифт: 32px, bold (900)
- Колір: var(--brass0) з тінню
- Формат: табулярні цифри
- Світіння: drop-shadow гло від стихії
- При виборі: змінюється на колір рідкості

---

### 6️⃣ **БЕЙДЖ РІДКОСТІ (top-right)**
```
Положення: top 16px, right 16px
Розмір: auto × 22px

СТИЛІЗАЦІЯ:
├─ Фон: rgba(0,0,0,0.8)
├─ Бордюр: 1px var(--rar)
├─ Текст: var(--rar) (яскравий)
├─ Шрифт: 10px, bold, uppercase, letter-spacing: 1px
└─ z-index: 3
```

**Тексти:**
- `ЗВИЧАЙНА` - common (сірий)
- `РІДКІСНА` - rare (синій)
- `ЕПІЧНА` - epic (фіолетовий)
- `ЛЕГЕНДА` - legend (золото)

---

## СТИХІЇ (--elem змінна)

| Стихія | Клас | Колір | HEX | Емодзі |
|--------|------|-------|-----|--------|
| Вогонь | `.fire` | #c45a2a | Оранжево-червоне | 🔥 |
| Вода | `.water` | #3b6c8e | Синє | 💧 |
| Повітря | `.air` | #9fb6c1 | Сизе | 💨 |
| Земля | `.earth` | #7a6a3a | Оливкове | 🪨 |
| Пара | `.steam` | #8a7f8a | Фіолетово-сіре | ☁️ |
| Механіка | `.mech` | #5a5a5a | Темно-сіре | ⚙️ |

---

## РІДКОСТІ (--rar змінна)

| Рідкість | Клас | Колір | HEX | Фільтр |
|----------|------|-------|-----|--------|
| Звичайна | `.common` | Піскова | #b8a27b | Повна непрозорість |
| Рідкісна | `.rare` | Синя | #6fb2ff | Блиск 10% |
| Епічна | `.epic` | Фіолетова | #b07cff | Блиск 15% |
| Легендарна | `.legend` | Золота | #ffcc66 | Гало 25px |

---

## СТАНИ КАРТИ

### 🖱️ **Normal State**
```
Шкала: 1.0
Яскравість: 1.0
Тінь: стандартна
Текст: звичайний
```

### 🖱️ **Hover State** (`:hover`)
```
Трансформація: translateY(-8px) scale(1.03)
├─ Піднімається на 8px вверх
└─ Збільшується на 3%

Тінь: посилена
└─ 0 20px 40px з глубшим гало

Шестерня: rotate(180deg)
Плашка сили: додаткове світіння
```

### 🖱️ **Selected State** (`.selected`)
```
Бордюр: 3px var(--rar) 
├─ Колір залежить від рідкості
└─ Світиться

Плашка сили: фон змінюється
├─ На колір рідкості (10-15% opacity)
└─ Текст міняє колір

Все світиться: color-mix(--rar, 50%)
```

---

## АНІМАЦІЇ

### ⚙️ **Rotating Gear**
```css
.corner-gear {
  animation: rotate(180deg) 0.6s ease;
  /* спрацьовує на :hover */
}
```

### ✨ **Subtle Glow**
```css
@keyframes subtle-glow {
  0%, 100% { filter: brightness(1.00); }
  50%      { filter: brightness(1.05); }
  /* Період: 3s */
  /* Запускається в фоні для всіх карт */
}
```

---

## ФІЛЬТРАЦІЯ

### Фільтри по елементам
- `data-filter="all"` → показати всі
- `data-filter="fire"` → тільки вогонь
- `data-filter="water"` → тільки вода
- `data-filter="air"` → тільки повітря
- `data-filter="earth"` → тільки земля
- `data-filter="legend"` → тільки легендарні

### CSS класи
- Клас стихії: `.fire`, `.water`, `.air`, `.earth`, `.steam`, `.mech`
- Клас рідкості: `.common`, `.rare`, `.epic`, `.legend`
- Стан: `.selected`

---

##響應НІСТЬ

### Desktop (> 768px)
- Сітка: `repeat(auto-fill, minmax(200px, 1fr))`
- Розмір карти: 200px × 280px
- Проміжок: 25px
- Шестерня: 48px

### Tablet (481px - 768px)
- Сітка: `repeat(auto-fill, minmax(150px, 1fr))`
- Розмір карти: 150px × 220px
- Проміжок: 20px
- Шестерня: 40px

### Mobile (< 480px)
- Сітка: `repeat(auto-fill, minmax(130px, 1fr))`
- Розмір карти: 130px × 200px
- Проміжок: 15px
- Шестерня: 36px

---

## КОЛЬОР-СХЕМА

```
--bg0:          #070504    /* Найтемніший фон */
--bg1:          #120c08    /* Темний фон */

--brass0:       #f2d07a    /* Яскрава латунь */
--brass1:       #c59b3c    /* Середня латунь */
--brass2:       #7a5520    /* Темна латунь */

--copper0:      #d8905b    /* Яскрава мідь */
--copper1:      #b56a33    /* Середня мідь */
--copper2:      #6f3316    /* Темна мідь */

--ink:          #f4e6c6    /* Основний текст */
--shadow:       rgba(0,0,0,.68)
```

---

## ІНТЕГРАЦІЯ В ГРУ

### Місця використання карт
1. **Deck Builder** (`page-deck`) - редактор колоди
2. **Battle Screen** - карти в бою
3. **Collection** (`page-collections`) - вся колекція гравця
4. **Card Gallery** - огляд всіх карт у грі

### Взаємодія
```javascript
// Клік на карту
card.addEventListener('click', () => {
  card.classList.toggle('selected');
  // Показати інформацію про карту
});

// Фільтрація
filterBtn.addEventListener('click', (e) => {
  const filter = e.target.dataset.filter;
  filterCards(filter);
});
```

---

## ШАБЛОН HTML

```html
<div class="sp-card fire legend" 
     data-element="fire" 
     data-rarity="legend">
  
  <!-- Бейдж рідкості -->
  <div class="rarity-badge">ЛЕГЕНДА</div>
  
  <!-- Шестерня зі стихією -->
  <div class="corner-gear">
    <svg viewBox="0 0 24 24">
      <!-- Стихійна іконка SVG -->
    </svg>
  </div>

  <!-- Плашка сили -->
  <div class="power-plate">
    <div class="power-value">28</div>
  </div>
  
</div>
```

---

## МАЙБУТНІ ПОЛІПШЕННЯ

- [ ] Назва карти в центрі
- [ ] Опис здібності під силою
- [ ] Анімація перевороту для повних деталей
- [ ] Звуки при виборі
- [ ] 3D перспектива при ховері
- [ ] Партиклі при виборі легендарної карти
