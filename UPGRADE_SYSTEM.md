# Система Прокачки Карт 🎮

## Загальний Опис

Реалізована система прокачки карт "як у колоді" на основі дублікатів карт. Лишні копії карт використовуються як "матеріал" для апгрейду основної карти в бойовій колоді.

---

## 📊 Модель Даних

### 1. **Інвентар (Inventory)**
Підраховується автоматично з карт у деці та колекції:
```javascript
const inventory = {
  "C-F-001": 3,   // всього 3 копії цієї карти
  "C-W-002": 1,   // всього 1 копія
}
```

### 2. **Колода (Deck)**
Масив із 9 слотів, кожна карта має рівень:
```javascript
profile.deckCards = [
  { id: "C-F-001", level: 2 },
  { id: "C-W-002", level: 1 },
  // ... 7 слотів
]
```

### 3. **Колекція (Collection)**
Лишні карти (не в деці):
```javascript
profile.collectionCards = [
  { id: "C-F-001", level: 1 },  // дублікат
  { id: "C-F-001", level: 1 },  // ще один дублікат
  { id: "C-W-003", level: 1 },
  // ...
]
```

---

## 🔧 Функції Системи

### `getInventory(profile)` 📦
Будує інвентар з рахунком усіх копій карт.
```javascript
const inventory = navigation.getInventory(profile);
// { "C-F-001": 3, "C-W-002": 1, ... }
```

### `getExtraCopies(inventory, cardId)` ➕
Повертає кількість лишніх копій (без основної в деці).
```javascript
const extra = navigation.getExtraCopies(inventory, "C-F-001");
// Якщо всього 3, то повертає 2 (тому що 1 в деці)
```

### `getUpgradeCost(level)` 💎
Вартість апгрейду з поточного рівня на наступний.
```javascript
getUpgradeCost(1) // = 1  (lvl 1→2 потребує 1 дублікат)
getUpgradeCost(2) // = 2  (lvl 2→3 потребує 2 дублікати)
getUpgradeCost(3) // = 3  (lvl 3→4 потребує 3 дублікати)
```

### `canUpgradeCard(deckItem, inventory)` ✅
Перевіряє, чи можна апгрейдити карту.
```javascript
const canUpgrade = navigation.canUpgradeCard(deckItem, inventory);
// true якщо є достатньо дублікатів
```

### `hasAnyUpgradable(deck, inventory)` 🔍
Перевіряє, чи в деці хоча б одна апгрейджвальна карта.
```javascript
const hasAny = navigation.hasAnyUpgradable(profile.deckCards, inventory);
```

### `performUpgrade(deckItem, inventory, profile)` ⚡
Виконує апгрейд:
1. Списує необхідну кількість дублікатів з колекції
2. Піднімає рівень карти в деці
3. Зберігає профіль
```javascript
const success = navigation.performUpgrade(deckItem, inventory, profile);
if (success) {
  console.log("Карта прокачена!");
}
```

---

## 🎨 UI Елементи

### Стрілка на Апгрейджвальній Карті ↑
Коли карту можна апгрейдити, на ній з'являється зелена стрілка з пульсуючим ефектом.

**CSS клас:** `.upgradable`
- Позиція: верхній правий кут карти
- Колір: зелений (#a6ff7a → #2ea13a)
- Ефект: пульсуючий box-shadow
- Анімація: `pulse-upgrade` (1.5s)

```css
.sp-card.upgradable::after {
  content: "↑";
  /* ... стилі ... */
  animation: pulse-upgrade 1.5s ease-in-out infinite;
}
```

### Гаряча Підказка "Поліпшуйте карти!" 🔥
Залежно від наявності апгрейджвальних карт:

**CSS клас:** `.hint.hot`
- Додаються: зелена свічення, яскравіший текст
- Коли показується: якщо `hasAnyUpgradable() === true`

```css
.hint.hot {
  border-color: rgba(120,255,160,.35);
  box-shadow: inset 0 0 14px rgba(0,0,0,.65), 0 0 18px rgba(90,255,120,.18);
  color: rgba(220,255,220,.95);
}
```

---

## 🎯 UX Механіка

### Клік на Карту
1. **Якщо можна апгрейдити:**
   - Апгрейд виконується одразу
   - Дублікати списуються
   - Рівень карти підвищується
   - Грід перерендерюється

2. **Якщо не можна апгрейдити:**
   - Карта вибирається (клас `.selected`)
   - Можна переглянути деталі

### Оновлення Стану
Після кожної дії грід повністю перерендерюється:
- Стрілки оновлюються на основі поточного інвентарю
- Підказка "Поліпшуйте карти!" оновлює свій стан
- Сила колоди перераховується

---

## 📈 Приклад Прокачки

**Початковий стан:**
- Колода: C-F-001 (lvl 1)
- Колекція: 2 дублікати C-F-001

**Інвентар:** `{ "C-F-001": 3 }`
**Лишні копії:** 2
**Вартість апгрейду:** 1
**Можна апгрейдити:** ✅ Так (є 2 лишніх, потребує 1)

↓ *Клік на карту* ↓

**Після апгрейду:**
- Колода: C-F-001 (lvl 2) ✨
- Колекція: 1 дублікат C-F-001
- Інвентар: `{ "C-F-001": 2 }`

↓ *Клік на карту знову* ↓

**Наступний апгрейд:**
**Вартість:** 2
**Можна апгрейдити:** ✅ Так (є 1 лишня, але потребує 2) ❌ Ні!

---

## 🔌 Інтеграція з Профілем

Функції системи прокачки доступні в об'єкті `navigation`:

```javascript
// У навігаційному об'єкті
const profile = userProfile.getProfile();
const inventory = navigation.getInventory(profile);

if (navigation.hasAnyUpgradable(profile.deckCards, inventory)) {
  // Показати підказку
}

// Апгрейдити карту
navigation.performUpgrade(deckItem, inventory, profile);
```

---

## 🚀 Тестування

### Перевірка Стрілок
1. Реєстрація нового користувача
2. Перейти на сторінку колоди
3. Мають бути видні стрілки на деяких картах
4. Клік на карту з стрілкою = апгрейд

### Перевірка Підказки
1. Якщо є апгрейджвальні карти → підказка "гаряча" (зелена)
2. Якщо немає апгрейджвальних → підказка звичайна

### Перевірка Вартості
1. Уповільнення апгрейдів (кожен рівень коштує більше)
2. На lvl 1→2 потребує 1 дублікат
3. На lvl 2→3 потребує 2 дублікати
4. На lvl 3→4 потребує 3 дублікати

---

## 💾 Збереження

Система автоматично зберігає в localStorage при кожному апгрейді:
```javascript
userProfile.updateCurrentUser(profile);
```

Дані зберігаються в `elem_users` ключ в локальному сховищі браузера.

---

## 🎓 Наступні Кроки

Можливі розширення:
1. **Перегляд Деталей Карти** - модальне вікно з інформацією
2. **Анімація Апгрейду** - фліш/частинки при апгрейді
3. **Батч-Апгрейди** - апгрейдити кілька карт відразу
4. **Звуки** - звуки при апгрейді
5. **Історія Апгрейдів** - лог змін рівнів

---

**Останнє оновлення:** 10 січня 2026
